import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const page=fs.readFileSync(new URL('../src/pages/TopRetailNetworksPage.tsx',import.meta.url),'utf8');test('aba Redes sobrescreve provenance estática com o build ativo',()=>{assert.ok(page.includes('motorBuildId: activeCanonical.motorBuildId'));assert.ok(page.includes('stagingManifestHash: activeCanonical.stagingManifestHash'));});
