import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const page=fs.readFileSync(new URL('../src/pages/TopRetailNetworksPage.tsx',import.meta.url),'utf8');test('ordem da tabela Redes segue definição operacional',()=>{const labels=['Rede','Clientes','Meta da rede','Meta Top Varejista','Ating. Meta Rede','Ating. Meta Top','Faturado','A faturar','Total','Participação'];let previous=-1;for(const label of labels){const next=page.indexOf(`>${label}<`);assert.ok(next>previous,`ordem inválida: ${label}`);previous=next;}});
