import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const page=fs.readFileSync(new URL('../src/pages/TopRetailNetworksPage.tsx',import.meta.url),'utf8');test('aba Redes não revive cards antigos',()=>{for(const label of ['Total nas redes','Faturado"','A faturar"','Redes com venda','Clientes vinculados'])assert.equal(page.includes(`label="${label}`),false);});
