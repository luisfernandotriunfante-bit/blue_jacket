import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const page=fs.readFileSync(new URL('../src/pages/TopRetailNetworksPage.tsx',import.meta.url),'utf8');test('cards da aba Redes são somente os quatro definidos',()=>{for(const label of ['Meta Redes','Realizado','Clientes × com venda','Gap de valor'])assert.ok(page.includes(`label="${label}"`));assert.equal((page.match(/<MetricCard/g)||[]).length,4);});
